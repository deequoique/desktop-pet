# Copy to a root-readable file, fill every required value, then chmod 600.
PUBLIC_IP=203.0.113.10
PRIVATE_IP=10.0.0.10
TURN_REALM=turn.example.com
TURN_SHARED_SECRET=replace-with-at-least-32-random-characters
APP_ENV_FILE=/opt/desktop-pet-server/.env

LISTENER_PORT=3478
RELAY_MIN_PORT=49160
RELAY_MAX_PORT=49200
MAX_BPS=64000
BPS_CAPACITY=250000
USER_QUOTA=4
TOTAL_QUOTA=8
ENABLE_TLS=false
TLS_PORT=5349
TLS_CERT_FILE=
TLS_KEY_FILE=
